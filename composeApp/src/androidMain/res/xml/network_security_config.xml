<?xml version="1.0" encoding="utf-8"?>
<!--
    Allows plain HTTP/WS (cleartext) to any host.
    This app connects to a user-configured local network IP address, so the
    server address is not known at build time and cannot be whitelisted by domain.
    Cleartext is acceptable here because all traffic stays on the private LAN.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>
    <debug-overrides>
        <trust-anchors>
            <certificates src="system"/>
            <certificates src="user"/>
        </trust-anchors>
    </debug-overrides>
</network-security-config>
